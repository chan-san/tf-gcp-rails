#!/bin/sh

ROOT_DIR=`cd $(dirname $0)/../..;pwd`
DOCKER_COMPOSE_ENV_FILE_NAME=docker-compose.env.yml
DOCKER_COMPOSE_ENV_FILE_PATH="$ROOT_DIR/$DOCKER_COMPOSE_ENV_FILE_NAME"

if [ -e $DOCKER_COMPOSE_ENV_FILE_PATH ]; then
  echo -f $DOCKER_COMPOSE_ENV_FILE_NAME
fi
